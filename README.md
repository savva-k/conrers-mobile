# Conrers mobile
Corners game for mobile platforms (will _probably_ be) written using React Native.

## Pre-requisites

* Node, NPM
* Expo (`npm install --global expo-cli`)

## Start development env

```
yarn start
```
